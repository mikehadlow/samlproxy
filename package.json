{
  "name": "samlproxy",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bunx tsc --noEmit; bun scripts/cluster.ts",
    "setup": "bun scripts/generateEnv.ts; bun scripts/generateKeys.ts"
  }
}
